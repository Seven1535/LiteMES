# LiteMES 轻量级制造执行系统 — 设计规格说明书

> 版本：v2.0 | 日期：2026-08-26 | 状态：微服务架构改造版

---

## 1. 产品概述

### 1.1 一句话定位

面向 10–50 人小型机械加工厂，**用得起、学得会、看得懂**的轻量级制造执行系统。

### 1.2 目标用户

| 角色 | 典型画像 | 核心诉求 |
|------|----------|----------|
| 厂长/管理员 | 一人管全厂，需要看全局 | 知道每张单做到哪了、谁在干什么 |
| 生产调度 | 负责排产派工 | 快速把工单分到工位，处理插单 |
| 车间工人 | 在工位上操作 | 看我该干什么，报完工 |
| 访客/面试演示 | 打开网站看 | 哇，这个流程图能拖拽，酷 |

### 1.3 解决什么问题

小型工厂现状：Excel 排产 + 纸质流转 + 微信群问进度。

LiteMES 提供：
- **工艺可视化**：拖拽画图定义工序流程，替代手写工艺卡
- **工单可追踪**：每张单走到哪一步，打开系统就知道
- **派工有记录**：谁干的、干了多久、合格多少，全部留痕
- **看板实时看**：车间大屏一挂，进度一目了然

### 1.4 系统形态

- **PC Web 应用**（主要），响应式布局兼容平板
- 单租户，部署在一台云服务器上
- 浏览器访问，无需安装客户端
- **微服务后端**：基础数据与业务数据拆分为两个独立服务，Gateway 统一入口鉴权（Nacos + Gateway + OpenFeign + Redis），前端无感知，详见《架构设计说明书》

---

## 2. 功能模块总览

```
LiteMES
├─ litemes-base（基础数据服务）
│   ├── 系统管理（System）      ← 用户/角色/权限 + JWT 认证签发
│   ├── 产品管理（Product）     ← 产品档案，关联工艺路线
│   ├── 工艺建模（Process）     ← 核心亮点：可视化流程图设计
│   └── 工位管理（WorkCenter）  ← 车间资源建模
│
└─ litemes-production（业务数据服务）
    ├── 生产工单（WorkOrder）   ← 生产任务下达与跟踪
    ├── 派工管理（Dispatch）    ← 任务分配到工位 + 报工
    └── 生产看板（Dashboard）   ← 实时进度可视化（聚合两服务数据）
```

> 划分原则：低频变化、被多方引用的主数据归基础数据服务；高频写入的交易数据归业务数据服务。两服务各自独立数据库，业务服务通过 OpenFeign 调用基础服务获取主数据。

### MVP 范围（第一版只做这些）

| 模块 | MVP 包含 | MVP 不做 |
|------|---------|---------|
| 工艺建模 | 工序定义、流程图可视化编辑、工序排序 | 工时标准、替代工艺 |
| 产品管理 | 产品 CRUD、关联工艺路线 | BOM 管理、物料需求计算 |
| 工位管理 | 工位 CRUD、状态管理 | 工位产能模型、设备关联 |
| 生产工单 | 工单 CRUD、状态流转、进度跟踪 | 插单逻辑、交期预警 |
| 派工管理 | 手动派工到工位、报工 | 自动排程、产能优化 |
| 生产看板 | 工单进度概览、工位负载概览 | 历史趋势分析、OEE |
| 系统管理 | 用户/角色/JWT 认证 | 操作日志、数据权限 |

---

## 3. 用户角色与权限

| 角色 | 编码 | 数据范围 | 主要操作 |
|------|------|---------|---------|
| 系统管理员 | ADMIN | 全部 | 所有功能 + 用户管理 |
| 生产调度 | DISPATCHER | 全部 | 工单管理、派工管理、看板 |
| 车间操作员 | OPERATOR | 本人 | 查看我的任务、报工 |
| 只读访客 | VIEWER | 全部 | 只看看板和产品/工艺信息 |

---

## 4. 核心业务流程

### 4.1 主流程

```
定义产品 → 设计工艺路线 → 创建工单 → 派工到工位 → 工人报工 → 工单完工
   │            │              │            │             │           │
   ▼            ▼              ▼            ▼             ▼           ▼
 PRODUCT    WORKFLOW      WORK_ORDER   DISPATCH     PROGRESS    COMPLETED
```

### 4.2 工单状态流转

```
                    ┌──────────┐
                    │  已创建   │ PLANNED
                    └────┬─────┘
                         │ 下达
                    ┌────▼─────┐         关闭
                    │  已下达   │ ──────────────┐
                    │ RELEASED │               │
                    └────┬─────┘               │
                         │ 全部派工             │
                    ┌────▼─────┐           ┌───▼──────┐
                    │  生产中   │   关闭    │  已关闭   │
                    │ IN_PROGRESS│ ────────▶│ CLOSED   │
                    └────┬─────┘           └──────────┘
                         │ 全部报工
                    ┌────▼─────┐
                    │  已完工   │ COMPLETED
                    └──────────┘

说明：
- 部分派工/报工 → 状态仍为 IN_PROGRESS
- 只有所有工序任务都完成，工单才进入 COMPLETED
- 已下达（RELEASED）/ 生产中（IN_PROGRESS）的工单可关闭，关闭后不可再派工
- 关闭时若存在未完成的派工任务，任务一并置为关闭（释放工位占用）
```

### 4.3 工序任务状态流转

```
  ┌──────────┐     派工      ┌──────────┐     报工      ┌──────────┐
  │  待派工   │ ──────────▶  │  加工中   │ ──────────▶  │  已完成   │
  │ PENDING  │              │ PROCESSING│              │ COMPLETED│
  └────┬─────┘              └────┬─────┘              └──────────┘
       │       工单关闭           │
       └──────────────┬──────────┘
                      ▼
              ┌──────────────┐
              │   已关闭      │ CLOSED
              └──────────────┘

说明：
- 报工为累计式：每次提交完工数量，累计达到派工数量时自动进入 COMPLETED
- 工单关闭时，PENDING / PROCESSING 的任务一并置为 CLOSED
```

### 4.4 工艺路线版本策略

- 每个产品对应一条工艺路线（1:1 关联）
- 工艺路线支持多版本：V1、V2、V3...
- 工单创建时锁定当时生效的版本，后续版本变更不影响已下达工单
- 同一产品同一时间只有一个"生效版本"

---

## 5. 数据模型设计

### 5.1 实体关系概览

```
USER ──┐
       │
PRODUCT ──1:1──▶ WORKFLOW ──1:N──▶ WORKFLOW_STEP
  │                  │                          ── 以上均在 litemes_base 库（基础数据服务）
  │                  │ (工单锁定版本)
  ▼                  ▼
WORK_ORDER ──1:N──▶ DISPATCH_TASK ──N:1──▶ WORK_CENTER
  │                          ── WORK_ORDER/DISPATCH_TASK 在 litemes_production 库（业务数据服务）
  └── 进度 = 所有 DISPATCH_TASK 完成比例

  跨库关联（如 DISPATCH_TASK.workCenterId）只存 ID，不加物理外键，
  由业务服务通过 OpenFeign 调基础服务校验与补全名称。
```

### 5.2 实体字段清单

#### SYS_USER（用户表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (UUID) | 主键 |
| username | String | 登录名，唯一 |
| password | String | BCrypt 加密 |
| realName | String | 真实姓名 |
| role | String | 角色编码（ADMIN/DISPATCHER/OPERATOR/VIEWER） |
| status | String | 状态（ACTIVE/DISABLED） |
| delFlag | String | 逻辑删除（0/1） |
| createdAt | LocalDateTime | 创建时间 |
| updatedAt | LocalDateTime | 更新时间 |
| createdBy / updatedBy | String | 审计人 |

#### PRODUCT（产品表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (UUID) | 主键 |
| productCode | String | 产品编码，唯一 |
| productName | String | 产品名称 |
| description | String | 产品描述 |
| drawingUrl | String | 图纸文件地址（可选） |
| status | String | 状态（ACTIVE/INACTIVE） |
| delFlag | String | 逻辑删除（0/1） |
| createdAt / updatedAt | LocalDateTime | 审计字段 |

#### WORKFLOW（工艺路线表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (UUID) | 主键 |
| productId | String | 关联产品 ID |
| version | Integer | 版本号（自增：1, 2, 3...） |
| versionName | String | 版本标识（如 "V1.0"） |
| description | String | 版本说明 |
| isActive | Boolean | 是否当前生效版本 |
| status | String | 状态（DRAFT/ACTIVE/ARCHIVED） |
| createdAt / updatedAt | LocalDateTime | 审计字段 |

#### WORKFLOW_STEP（工序步骤表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (UUID) | 主键 |
| workflowId | String | 所属工艺路线 ID |
| stepCode | String | 工序编码 |
| stepName | String | 工序名称（如"粗车"、"精磨"、"检验"） |
| stepOrder | Integer | 工序顺序号 |
| description | String | 工序描述 |
| requiredWorkCenterType | String | 要求工位类型（可选，用于派工推荐） |
| estimatedMinutes | Integer | 预估工时（分钟） |
| posX | Double | 流程图中的 X 坐标（画布定位用） |
| posY | Double | 流程图中的 Y 坐标 |
| createdAt / updatedAt | LocalDateTime | 审计字段 |

#### WORK_CENTER（工位表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (UUID) | 主键 |
| centerCode | String | 工位编码，唯一 |
| centerName | String | 工位名称 |
| centerType | String | 工位类型（如"车床"、"铣床"、"检验台"） |
| operatorId | String | 当前操作员 ID |
| status | String | 状态（IDLE/BUSY/OFFLINE） |
| delFlag | String | 逻辑删除（0/1） |
| createdAt / updatedAt | LocalDateTime | 审计字段 |

#### WORK_ORDER（生产工单表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (UUID) | 主键 |
| orderNo | String | 工单编号（自动生成：WO-yyyyMMdd-NNN） |
| productId | String | 产品 ID |
| workflowId | String | 锁定的工艺路线版本 ID |
| quantity | Integer | 计划数量 |
| completedQty | Integer | 已完工数量（默认 0） |
| priority | Integer | 优先级（1=紧急, 2=高, 3=中, 4=低） |
| status | String | 状态（PLANNED/RELEASED/IN_PROGRESS/COMPLETED/CLOSED） |
| planStartDate | LocalDate | 计划开始日期 |
| planEndDate | LocalDate | 计划完成日期 |
| actualStartDate | LocalDate | 实际开始日期 |
| actualEndDate | LocalDate | 实际完成日期 |
| remark | String | 备注 |
| delFlag | String | 逻辑删除（0/1） |
| createdAt / updatedAt | LocalDateTime | 审计字段 |
| createdBy / updatedBy | String | 审计人 |

#### DISPATCH_TASK（派工任务表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | String (UUID) | 主键 |
| taskNo | String | 任务编号（自动生成：TK-yyyyMMdd-NNNN） |
| workOrderId | String | 所属工单 ID |
| workflowStepId | String | 对应工序 ID |
| workCenterId | String | 分配到的工位 ID |
| operatorId | String | 操作员 ID |
| quantity | Integer | 派工数量 |
| completedQty | Integer | 完工数量（默认 0） |
| status | String | 状态（PENDING/PROCESSING/COMPLETED/CLOSED） |
| dispatchedAt | LocalDateTime | 派工时间 |
| startedAt | LocalDateTime | 开工时间 |
| completedAt | LocalDateTime | 完工时间 |
| remark | String | 备注 |
| createdAt / updatedAt | LocalDateTime | 审计字段 |

### 5.3 编号生成规则

| 编号类型 | 格式 | 示例 | 唯一性保障 |
|---------|------|------|-----------|
| 工单编号 | `WO-{yyyyMMdd}-{3位序号}` | WO-20260826-001 | Redis INCR 原子自增（key 按日期隔离）+ 数据库唯一约束兜底 |
| 任务编号 | `TK-{yyyyMMdd}-{4位序号}` | TK-20260826-0001 | Redis INCR 原子自增 + 数据库唯一约束兜底 |
| 产品编码 | 用户自定义 | P-001 | 唯一约束 |
| 工位编码 | 用户自定义 | WC-LATHE-01 | 唯一约束 |

---

## 6. 界面设计

### 6.1 整体布局

```
┌──────────────────────────────────────────────────────┐
│  LiteMES                    [通知] [用户名] [退出]    │
├──────────┬───────────────────────────────────────────┤
│          │                                           │
│  📋 工艺  │                                           │
│  📦 产品  │            主内容区域                      │
│  🏭 工位  │                                           │
│  📝 工单  │                                           │
│  🔧 派工  │                                           │
│  📊 看板  │                                           │
│  ⚙️ 系统  │                                           │
│          │                                           │
└──────────┴───────────────────────────────────────────┘
```

### 6.2 页面清单

| 页面 | 路由 | 功能 | 角色 |
|------|------|------|------|
| 登录页 | /login | 用户名密码登录 | 所有 |
| 首页/看板 | /dashboard | 生产进度概览 | 所有（操作员只看自己的） |
| 产品列表 | /product | 产品 CRUD | ADMIN, DISPATCHER |
| 工艺路线编辑 | /process/:productId | 可视化流程图设计 | ADMIN, DISPATCHER |
| 工位列表 | /workcenter | 工位 CRUD + 状态 | ADMIN |
| 工单列表 | /workorder | 工单 CRUD + 状态流转 | ADMIN, DISPATCHER |
| 工单详情 | /workorder/:id | 工单详情 + 工序进度 | 所有 |
| 派工管理 | /dispatch | 派工任务列表 + 操作 | ADMIN, DISPATCHER |
| 我的任务 | /my-tasks | 操作员看自己的任务 + 报工 | OPERATOR |
| 用户管理 | /system/user | 用户 CRUD | ADMIN |

### 6.3 核心页面详细设计

#### 6.3.1 工艺路线可视化编辑器（核心亮点）

这是整个系统最"有面子"的页面，面试演示的重点。

**布局**：三段式

```
┌──────────────────────────────────────────────────┐
│ 工具栏：[保存] [发布] [添加版本]  当前版本: V1.0    │
├──────────┬───────────────────────────┬───────────┤
│ 工序面板  │      画布区域              │  属性面板  │
│          │                           │           │
│ ○ 粗车   │   [○ 粗车] ──▶ [○ 精磨]   │ 工序名称   │
│ ○ 精磨   │              │            │ 工序编码   │
│ ○ 检验   │           [○ 检验]        │ 预估工时   │
│ ○ 热处理  │                           │ 要求工位   │
│          │                           │ 工序描述   │
│ [+新增]  │                           │           │
├──────────┴───────────────────────────┴───────────┤
│ 状态栏：已保存 | 共 4 道工序 | 最后修改: 10:30     │
└──────────────────────────────────────────────────┘
```

**交互设计**：
- 左侧工序面板：列出当前所有工序，可拖拽到画布添加
- 中间画布：显示工序节点和连线，支持拖拽移动位置
- 右侧属性面板：点击某个工序节点后，显示/编辑其属性
- 工具栏：保存草稿、发布生效、新增版本
- 连线：工序之间用箭头连接，表示先后顺序

**技术方案**：
- 前端使用 Vue Flow（基于 React Flow 的 Vue 3 移植版）实现画布
- 节点位置保存在 WORKFLOW_STEP 的 posX/posY 字段
- 连线关系用单独的 WORKFLOW_STEP_LINK 表保存（前驱/后继）

#### 6.3.2 生产看板

```
┌──────────────────────────────────────────────────┐
│  生产看板          [刷新]  日期: 2026-08-26       │
├──────────────────────────────────────────────────┤
│                                                  │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐          │
│  │ 工单概览 │  │ 工位状态 │  │ 今日产出 │          │
│  │ 进行中 5 │  │ 空闲  3 │  │ 完成 12 │          │
│  │ 待下达 2 │  │ 忙碌  5 │  │ 计划 20 │          │
│  │ 已完成 8 │  │ 离线  1 │  │ 达成 60%│          │
│  └─────────┘  └─────────┘  └─────────┘          │
│                                                  │
│  ┌──────────────────────────────────────────┐    │
│  │         在制工单进度条                      │    │
│  │                                          │    │
│  │  WO-0826-001  零件A  ████████░░  80%     │    │
│  │  WO-0826-002  零件B  ████░░░░░░  40%     │    │
│  │  WO-0825-003  零件C  ██████████  100% ✓  │    │
│  │  WO-0826-004  零件D  ██░░░░░░░░  20%     │    │
│  └──────────────────────────────────────────┘    │
│                                                  │
│  ┌──────────────────────────────────────────┐    │
│  │         工位负载看板                       │    │
│  │                                          │    │
│  │  车床-01  张三  [WO-0826-001 粗车] 进行中  │    │
│  │  车床-02  李四  [WO-0826-002 粗车] 进行中  │    │
│  │  铣床-01  空闲                            │    │
│  │  检验台   王五  [WO-0825-003 检验] 进行中  │    │
│  └──────────────────────────────────────────┘    │
│                                                  │
└──────────────────────────────────────────────────┘
```

**实时更新**：
- 使用 WebSocket 推送，工位报工后看板自动刷新
- 前端断线后自动重连，重连时全量拉取一次

#### 6.3.3 派工操作

```
┌──────────────────────────────────────────────────┐
│  派工管理                                         │
├──────────────────────────────────────────────────┤
│  筛选：[工单编号____] [工位____] [状态 ▼]  [查询]  │
│                                                  │
│  ┌──────────────────────────────────────────────┐│
│  │ 任务编号     │ 工单       │ 工序 │ 工位 │ 状态 ││
│  ├──────────────┼───────────┼─────┼─────┼─────┤│
│  │ TK-0826-0001 │ WO-0826-001│粗车 │车床01│加工中││
│  │ TK-0826-0002 │ WO-0826-001│精磨 │ —   │待派工 ││
│  │ TK-0826-0003 │ WO-0826-002│粗车 │铣床01│已完成││
│  └──────────────────────────────────────────────┘│
│                                                  │
│  点击"待派工"行的 [派工] 按钮，弹出派工弹窗：       │
│                                                  │
│  ┌─────────────────────────────┐                 │
│  │ 派工                         │                 │
│  │ 工序：精磨                    │                 │
│  │ 数量：[____]                 │                 │
│  │ 工位：[选择 ▼] (推荐匹配类型) │                 │
│  │ 操作员：[选择 ▼]             │                 │
│  │          [确认派工] [取消]    │                 │
│  └─────────────────────────────┘                 │
└──────────────────────────────────────────────────┘
```

#### 6.3.4 操作员 — 我的任务

```
┌──────────────────────────────────────────────────┐
│  我的任务（张三 · 车床-01）                         │
├──────────────────────────────────────────────────┤
│                                                  │
│  ┌─ 进行中 ─────────────────────────────────────┐│
│  │                                              ││
│  │  📌 WO-0826-001 · 零件A · 粗车               ││
│  │  派工数量: 100 | 已完工: 60                    ││
│  │  [报工 +10] [报工 +20] [自定义数量...]         ││
│  │                                              ││
│  └──────────────────────────────────────────────┘│
│                                                  │
│  ┌─ 待处理 ─────────────────────────────────────┐│
│  │                                              ││
│  │  WO-0826-004 · 零件D · 粗车                   ││
│  │  派工数量: 50                                 ││
│  │  [开始加工]                                   ││
│  │                                              ││
│  └──────────────────────────────────────────────┘│
│                                                  │
│  ┌─ 已完成 ─────────────────────────────────────┐│
│  │  WO-0825-003 · 零件C · 粗车  ✓ 完成于 08-25  ││
│  └──────────────────────────────────────────────┘│
└──────────────────────────────────────────────────┘
```

---

## 7. 接口设计概要

### 7.1 接口规范

- 统一前缀：基础数据服务 `/api/v1/base/`，业务数据服务 `/api/v1/prod/`
- 所有请求经 Spring Cloud Gateway（8080）统一入口：网关完成 JWT 鉴权过滤（白名单除外）后，按路径前缀路由到对应服务；前端和 Nginx 均不感知后端服务拓扑
- 认证方式：JWT Bearer Token（登录接口除外，所有接口需携带 Token）；服务间 Feign 调用不走网关，由拦截器自动透传身份信息
- 统一响应格式：

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

- 分页响应格式：

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "rows": [ ... ],
    "total": 100,
    "pageNum": 1,
    "pageSize": 20
  }
}
```

### 7.2 核心接口清单

#### 认证模块（litemes-base）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/base/auth/login | 登录，返回 JWT Token |
| GET | /api/v1/base/auth/me | 获取当前用户信息 |

#### 产品模块（litemes-base）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/base/products | 产品列表（分页 + 筛选） |
| GET | /api/v1/base/products/{id} | 产品详情 |
| POST | /api/v1/base/products | 创建产品 |
| PUT | /api/v1/base/products/{id} | 更新产品 |
| DELETE | /api/v1/base/products/{id} | 删除产品（逻辑删除，先经 Feign 检查工单引用） |

#### 工艺路线模块（litemes-base）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/base/products/{productId}/workflows | 产品的工艺路线版本列表 |
| GET | /api/v1/base/workflows/{id} | 工艺路线详情（含工序步骤和连线） |
| POST | /api/v1/base/products/{productId}/workflows | 新建工艺路线版本 |
| PUT | /api/v1/base/workflows/{id} | 更新工艺路线（工序增删改、位置更新） |
| PUT | /api/v1/base/workflows/{id}/activate | 发布/激活某版本 |
| PUT | /api/v1/base/workflows/{id}/steps | 批量保存工序（画布保存） |
| PUT | /api/v1/base/workflows/{id}/links | 批量保存连线关系 |

#### 工位模块（litemes-base）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/base/workcenters | 工位列表 |
| POST | /api/v1/base/workcenters | 创建工位 |
| PUT | /api/v1/base/workcenters/{id} | 更新工位 |
| DELETE | /api/v1/base/workcenters/{id} | 删除工位（先经 Feign 检查进行中任务） |
| PUT | /api/v1/base/workcenters/{id}/status | 更新工位状态 |

#### 用户管理模块（litemes-base）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/base/users | 用户列表（分页 + 筛选，仅 ADMIN） |
| POST | /api/v1/base/users | 创建用户（密码 BCrypt 加密） |
| PUT | /api/v1/base/users/{id} | 更新用户（角色/状态） |
| DELETE | /api/v1/base/users/{id} | 删除用户 |
| PUT | /api/v1/base/users/{id}/reset-password | 重置密码 |

#### 工单模块（litemes-production，创建时经 Feign 校验产品与工艺版本）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/prod/workorders | 工单列表（分页 + 筛选，产品/工艺名称经 Feign 聚合） |
| GET | /api/v1/prod/workorders/{id} | 工单详情（含工序任务进度） |
| POST | /api/v1/prod/workorders | 创建工单（Redis INCR 生成工单号，Feign 锁定生效工艺版本） |
| PUT | /api/v1/prod/workorders/{id} | 更新工单 |
| DELETE | /api/v1/prod/workorders/{id} | 删除工单 |
| PUT | /api/v1/prod/workorders/{id}/release | 下达工单 |
| PUT | /api/v1/prod/workorders/{id}/close | 关闭工单 |

#### 派工模块（litemes-production，派工时经 Feign 校验工位/操作员）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/prod/dispatch-tasks | 派工任务列表 |
| POST | /api/v1/prod/dispatch-tasks | 创建派工任务（派工） |
| PUT | /api/v1/prod/dispatch-tasks/{id}/start | 开始加工 |
| PUT | /api/v1/prod/dispatch-tasks/{id}/report | 报工（提交完工数量，累计达到派工数量自动完成；Redisson 分布式锁防并发重复报工） |

#### 看板模块（litemes-production，聚合 Feign 主数据 + Redis 缓存）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/prod/dashboard/summary | 概览数据（在制数、完成数、工位状态统计） |
| GET | /api/v1/prod/dashboard/workorders | 在制工单进度列表 |
| GET | /api/v1/prod/dashboard/workcenters | 工位负载列表 |
| WS | /ws/production | WebSocket 实时推送生产事件（由 production 服务承载） |

#### 内部接口（仅供 Feign 调用，不对前端暴露）

| 服务 | 方法 | 路径 | 说明 |
|------|------|------|------|
| base | GET | /inner/products/{id} | 产品详情（契约 ProductClient） |
| base | GET | /inner/workflows/{productId}/active | 当前生效工艺版本 |
| base | GET | /inner/workflow-steps/{id} | 工序详情 |
| base | GET | /inner/workcenters | 工位列表（可按类型筛选，用于派工推荐） |
| base | GET | /inner/users/{id} | 用户详情 |
| production | GET | /inner/references/products/{productId} | 产品被工单引用计数（删除保护） |
| production | GET | /inner/references/workcenters/{workCenterId} | 工位进行中任务数（删除保护） |

---

## 8. 业务规则与约束

### 8.1 工艺建模

- 产品编码全局唯一，创建后不可修改
- 工艺路线必须至少包含 1 道工序才能发布
- 同一产品同一时间只能有一个"生效"版本的工艺路线
- 已激活版本不可再编辑（需创建新版本）
- 工序编码在同一工艺路线内唯一

### 8.2 工单管理

- 工单编号自动生成，不可手动修改
- 创建工单时自动锁定当前产品的生效工艺版本
- 只有 PLANNED 状态的工单可以下达
- 只有所有工序任务都 COMPLETED，工单才能标记为 COMPLETED
- 已下达的工单不可删除（只能关闭）
- 已关闭（CLOSED）的工单不可再编辑、派工、报工，也不可删除
- 已完工的工单不可再派工

### 8.3 派工管理

- 派工时必须选择工位和操作员
- 派工数量不能超过工单剩余未完成数量
- 同一工序可以被派到不同工位（拆分派工）
- 报工数量不能超过派工数量
- 报工后自动更新工单的 completedQty
- WebSocket 推送报工事件给看板

### 8.4 删除保护（跨服务校验）

| 实体 | 删除规则 | 校验方式 |
|------|---------|---------|
| 产品 | 存在关联工单时禁止删除 | base 经 Feign 调 production 引用计数接口 |
| 工艺路线 | 已激活版本禁止删除，只能归档 | base 本地校验 |
| 工位 | 存在进行中的派工任务时禁止删除 | base 经 Feign 调 production 引用计数接口 |
| 工单 | 只有 PLANNED 状态可删除 | production 本地校验 |
| 派工任务 | 只有 PENDING 状态可删除 | production 本地校验 |

> 删除保护的反向 Feign 调用配置 fallback：若 production 服务不可用，删除请求直接拒绝（安全优先，宁可拒绝不可误删）。

---

## 9. 非功能需求

| 维度 | 要求 |
|------|------|
| 性能 | 页面加载 < 2s，接口响应 < 500ms（含 Feign 跨服务调用开销） |
| 并发 | 支持 20 人同时在线操作 |
| 安全 | Gateway 统一 JWT 鉴权过滤（含 Redis 黑名单强制下线）+ 下游服务兜底校验 + BCrypt 密码加密 + 跨域网关统一收口；内部 Feign 接口仅允许服务间访问 |
| 可用性 | 单机 Docker Compose 部署，两服务故障隔离；数据库每日自动备份 |
| 兼容性 | Chrome 90+、Edge 90+、Firefox 90+ |
| 数据备份 | MySQL 每日凌晨自动 mysqldump（两个库分别备份） |

---

## 10. 后续扩展方向（MVP 不做）

- **BOM 管理**：物料清单 + 物料需求自动计算
- **质量管理**：检验记录、不合格品处理
- **自动排程**：基于产能和交期的自动排产建议
- **移动端报工**：微信小程序或 H5 页面
- **设备集成**：对接 PLC/CNC 自动采集加工数据
- **多租户**：SaaS 化，多家工厂共用一套系统
- **数据分析**：OEE、产出趋势、工位效率分析图表
