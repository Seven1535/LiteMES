# LiteMES 轻量级制造执行系统 — 架构设计说明书

> 版本：v2.0 | 日期：2026-08-26 | 状态：微服务架构改造版

---

## 1. 架构总览

### 1.1 设计原则

- **微服务架构**：按数据属性拆分为"基础数据服务"与"业务数据服务"两个独立服务，学习并展示 Spring Cloud 核心技术栈
- **技术栈对标企业**：Nacos 注册/配置中心 + OpenFeign 服务间调用 + Redis 缓存，与主流招聘要求一致
- **服务自治**：每个服务独立数据库 Schema、独立部署、独立版本迭代
- **前后端分离**：前端 Vue 3 SPA，后端 RESTful API，独立部署
- **容器化部署**：Docker Compose 一键启动，云服务器可复现

### 1.1.1 为什么这样拆分（面试必讲）

| 拆分依据 | 说明 |
|---------|------|
| 基础数据服务（litemes-base） | 用户、产品、工艺路线、工位——变化频率低、被多方引用的"主数据"，强调数据准确性与一致性 |
| 业务数据服务（litemes-production） | 工单、派工任务、报工、看板——高频写入的"交易数据"，强调并发处理与实时性 |
| 拆分收益 | 两类数据读写特征不同，可独立扩容；故障隔离（业务服务挂了不影响基础数据查询）；接口契约由 Feign Client 定义，天然练习接口设计能力 |

### 1.2 整体架构图

```
                              浏览器
                                │
                                │ HTTP
                                ▼
                         ┌──────────────┐
                         │    Nginx     │  静态资源 + 反向代理
                         │     (80)     │
                         └──────┬───────┘
                                │  /api/** 与 /ws/** 统一转发给网关
                                ▼
              ┌─────────────────────────────────────────┐
              │     litemes-gateway（Spring Cloud Gateway）    │
              │                (8080)                          │
              │   统一入口：路由转发 + JWT 鉴权过滤 + 跨域处理     │
              │   路由规则：                                     │
              │    /api/v1/base/** → lb://litemes-base         │
              │    /api/v1/prod/** → lb://litemes-production   │
              │    /ws/**          → lb://litemes-production   │
              └──────────┬──────────────────┬───────────┘
                         │                  │
            /api/v1/base/*                  */api/v1/prod/*  /ws/*
                         ▼                  ▼
   ┌────────────────────┐ ┌────────────────────┐
   │  litemes-base      │ │ litemes-production │
   │  基础数据服务        │ │ 业务数据服务        │
   │  (8081)            │ │ (8082，含 WebSocket) │
   │  用户/产品/工艺/工位 │ │ 工单/派工/报工/看板  │
   └─────────┬──────────┘ └────┬───────▲───────┘
             │                 │       │
             │◀─── OpenFeign + LoadBalancer 调用 ───┤
             │     （业务服务查询产品/工序/工位主数据） │
             ▼                                     │
   ┌──────────────────────────────────────────────────────┐
   │                     MySQL 8（单实例，双 Schema）       │
   │        litemes_base 库        litemes_production 库   │
   └──────────────────────────────────────────────────────┘
   ┌──────────────┐   ┌──────────────┐
   │  Redis 7     │   │  Nacos 2.3   │
   │   (6379)     │   │   (8848)     │
   │ JWT黑名单     │   │ 注册中心      │
   │ 看板缓存/分布式锁│   │ 配置中心      │
   └──────────────┘   └──────────────┘
```

**服务间调用关系**（只允许单向依赖：production → base）：

| 调用场景 | Feign 接口 | 说明 |
|---------|-----------|------|
| 创建工单 | ProductClient / WorkflowClient | 校验产品存在、锁定生效工艺版本 |
| 派工 | WorkCenterClient / UserClient | 校验工位状态、获取操作员信息、按类型推荐工位 |
| 报工 | WorkflowStepClient | 获取工序信息用于进度汇总 |
| 删除保护 | ReferenceCheckClient（base 反向查 production） | 删除产品/工位前由 base 调 production 的"引用计数查询"接口检查引用，这是唯一的反向调用 |

> 注意：base 对 production 的删除保护校验是唯一反向调用，统一由 ReferenceCheckClient 契约定义，避免双向强依赖。

### 1.3 部署架构

```
云服务器（2C4G 建议配置，最低 2C2G）
├── Docker Compose
│   ├── nginx              (80)      ── 前端静态资源 + 反向代理到网关
│   ├── litemes-gateway    (8080)    ── Spring Cloud Gateway 统一入口（路由 + 鉴权过滤）
│   ├── litemes-base       (8081)    ── 基础数据服务
│   ├── litemes-production (8082)    ── 业务数据服务（含 WebSocket）
│   ├── mysql              (3306)    ── MySQL 8.0，含 litemes_base / litemes_production 两个库
│   ├── redis              (6379)    ── Redis 缓存
│   └── nacos              (8848)    ── Nacos 注册中心 + 配置中心（standalone 模式）
│
├── 数据卷
│   ├── mysql-data/                  ── 数据库持久化
│   ├── redis-data/                  ── Redis 持久化
│   ├── nacos-data/                  ── Nacos 数据持久化
│   └── backups/                     ── 每日数据库备份
│
└── 配置文件
    ├── docker-compose.yml
    ├── nginx/nginx.conf
    └── .env                         ── 环境变量（密码等敏感信息）
```

**启动顺序依赖链**：`MySQL/Redis → Nacos → litemes-base / litemes-production → litemes-gateway → Nginx`

---

## 2. 技术栈选型

### 2.1 后端

| 组件 | 版本 | 选型理由 |
|------|------|---------|
| JDK | 17 | LTS 版本，当前主流 |
| Spring Boot | 3.2.5 | 当前主流版本，与工作中项目一致 |
| Spring Cloud | 2023.0.2 | 与 Boot 3.2 配套的微服务框架 |
| Spring Cloud Alibaba | 2023.0.1.2 | Nacos 集成（与 Spring Cloud 2023 兼容的官方版本） |
| Nacos | 2.3.x | 服务注册发现 + 分布式配置中心，国内微服务事实标准 |
| OpenFeign | 随 Cloud | 声明式 HTTP 客户端，服务间调用 |
| Spring Cloud LoadBalancer | 随 Cloud | 客户端负载均衡（Ribbon 的官方替代品） |
| Spring Cloud Gateway | 随 Cloud | 微服务统一入口：动态路由（lb:// 服务发现）、JWT 全局鉴权过滤、跨域统一处理 |
| Spring Data JPA | 随 Boot | ORM 层，开发效率高 |
| Spring Security | 随 Boot | 认证授权框架 |
| MySQL | 8.0 | 最广泛使用的开源数据库，面试通用 |
| Redis | 7.x | JWT 黑名单 + 看板缓存 + 分布式锁 |
| Redisson | 3.27.x | 分布式锁（工单号/任务号生成、报工并发控制） |
| JJWT | 0.12.6 | JWT Token 生成与校验 |
| SpringDoc OpenAPI | 2.3.0 | 自动生成 API 文档（Swagger UI） |
| Lombok | 1.18.34 | 减少样板代码 |
| MapStruct | 1.5.5 | Entity ↔ DTO 映射 |
| Flyway | 随 Boot | 数据库版本迁移管理（每个服务独立管理自己的库） |

**不引入的**：
- 不用 Sentinel/Seata：学习项目不追求完整治理，熔断降级用 OpenFeign fallback 兜底即可；分布式事务用"接口幂等 + 最终一致"方案替代，面试反而是加分讲点
- 不用 QueryDSL：MVP 阶段 JPA 方法名查询 + JPQL 足够
- 不用 MinIO/AblazeFS：MVP 不需要文件存储，图纸地址存字符串即可

### 2.2 前端

| 组件 | 版本 | 选型理由 |
|------|------|---------|
| Vue | 3.5+ | 当前主流，与工作中项目一致 |
| Vue Router | 4.x | SPA 路由 |
| Pinia | 2.x | 状态管理（比 Vuex 更轻量） |
| Element Plus | 2.10+ | UI 组件库，与工作中项目一致 |
| Axios | 1.7+ | HTTP 请求 |
| Vue Flow | 1.x | 流程图/画布（工艺路线可视化核心） |
| Vite | 5.x | 构建工具（比 Vue CLI 快） |
| Sass | 1.x | CSS 预处理 |

**选型说明**：
- 用 **Pinia** 替代 Vuex：Vue 3 官方推荐，API 更简洁
- 用 **Vite** 替代 Vue CLI：构建速度快 10 倍，新项目首选
- **Vue Flow** 是工艺路线可视化编辑器的核心，基于 SVG 渲染，支持拖拽、连线、缩放

### 2.3 基础设施

| 组件 | 用途 |
|------|------|
| Nginx | 前端静态资源托管 + 反向代理到 Gateway（不再直接对接业务服务） + WebSocket 代理 |
| Docker + Docker Compose | 容器化部署，一键启动全部环境 |
| Let's Encrypt | 免费 HTTPS 证书（可选，启用时需给 Nginx 补 443 监听与证书配置） |

---

## 3. 后端架构详细设计

### 3.1 工程结构（Maven 多模块）

```
lite-mes/
├── pom.xml                              # 父 POM：统一版本管理（Boot/Cloud/Alibaba BOM）
│
├── litemes-common/                      # 公共模块（被两个服务依赖，不打可执行 jar）
│   └── src/main/java/com/litemes/common/
│       ├── core/                        # BaseEntity、AjaxResult、PageResult、BusinessException
│       ├── exception/                   # GlobalExceptionHandler
│       ├── security/                    # JwtTokenProvider、JwtAuthFilter（两服务共用）
│       ├── redis/                       # Redis 工具、Redisson 配置
│       └── code/                        # CodeGenerator（基于 Redis 自增的编号生成器）
│
├── litemes-gateway/                     # 网关服务（8080）
│   └── src/main/java/com/litemes/gateway/
│       ├── GatewayApplication.java
│       ├── filter/
│       │   └── AuthGlobalFilter.java    # JWT 统一鉴权全局过滤器（核心练习点）
│       └── config/
│           └── CorsConfig.java          # 跨域由网关统一处理
│
├── litemes-api/                         # Feign 接口契约模块（只含接口 + DTO）
│   └── src/main/java/com/litemes/api/
│       ├── base/                        # base 服务对外契约
│       │   ├── ProductClient.java
│       │   ├── WorkflowClient.java
│       │   ├── WorkflowStepClient.java
│       │   ├── WorkCenterClient.java
│       │   ├── UserClient.java
│       │   └── dto/                     # 契约 DTO（ProductDTO、WorkflowStepDTO 等）
│       └── production/                  # production 服务对外契约（引用计数查询）
│           ├── ReferenceCheckClient.java
│           └── dto/
│
├── litemes-base/                        # 基础数据服务（8081）
│   └── src/main/java/com/litemes/base/
│       ├── BaseApplication.java         # @EnableFeignClients @EnableDiscoveryClient
│       ├── config/                      # SecurityConfig、CorsConfig、FlywayConfig
│       ├── module/
│       │   ├── auth/                    # 登录认证（JWT 签发）
│       │   ├── user/                    # 用户管理
│       │   ├── product/                 # 产品管理
│       │   ├── process/                 # 工艺建模（Workflow/WorkflowStep/WorkflowStepLink）
│       │   └── workcenter/              # 工位管理
│       └── feign/                       # ReferenceCheckClient 的 fallback 与内部调用
│   └── src/main/resources/
│       ├── bootstrap.yml                # Nacos 地址 + 服务名 litemes-base
│       └── db/migration/                # Flyway：V1__create_sys_user.sql 等（只管 litemes_base 库）
│
├── litemes-production/                  # 业务数据服务（8082）
│   └── src/main/java/com/litemes/production/
│       ├── ProductionApplication.java   # @EnableFeignClients @EnableDiscoveryClient
│       ├── config/                      # SecurityConfig、WebSocketConfig、FlywayConfig
│       ├── module/
│       │   ├── workorder/               # 工单管理
│       │   ├── dispatch/                # 派工 + 报工
│       │   ├── dashboard/               # 看板（聚合 Feign 主数据 + 本地业务数据）
│       │   └── reference/               # 引用计数查询接口（供 base 删除保护调用）
│       ├── feign/                       # ProductClient/WorkflowClient 等的 fallback 实现
│       └── websocket/                   # ProductionEventHandler、ProductionEvent
│   └── src/main/resources/
│       ├── bootstrap.yml                # Nacos 地址 + 服务名 litemes-production
│       └── db/migration/                # Flyway：只管 litemes_production 库
│
└── docker/
    ├── Dockerfile.gateway               # 网关服务镜像
    ├── Dockerfile.base                  # 基础数据服务镜像
    ├── Dockerfile.production            # 业务数据服务镜像
    └── docker-compose.yml               # 全部服务编排
```

**Nacos 配置中心约定**：

| 配置文件 | 内容 |
|---------|------|
| litemes-gateway.yml | 网关路由规则 + 鉴权白名单路径 |
| litemes-base.yml | base 服务私有配置（数据源 litemes_base 库） |
| litemes-production.yml | production 服务私有配置（数据源 litemes_production 库） |
| application-common.yml | 共享配置：JWT 密钥、Redis 连接、统一日志格式 |

### 3.2 分层职责

| 层 | 职责 | 约束 |
|----|------|------|
| Controller | 接收请求、参数校验、调用 Service、返回响应 | 不写业务逻辑 |
| Service | 业务逻辑、事务管理、调用 Repository | 核心业务集中在此 |
| Repository | 数据访问、JPQL 查询 | 不包含业务逻辑 |
| Entity | 数据库表映射 | 继承 BaseEntity |
| DTO | 接口入参/出参 | 与 Entity 分离，不暴露内部结构 |
| VO | 前端展示对象 | 只用于只读展示场景 |

### 3.3 统一响应体

```java
public class AjaxResult<T> {
    private int code;       // 200=成功, 400=参数错误, 401=未认证, 403=无权限, 404=资源不存在, 500=系统错误
    private String message; // 提示信息
    private T data;         // 业务数据

    public static <T> AjaxResult<T> success(T data) { ... }
    public static <T> AjaxResult<T> error(int code, String message) { ... }
}
```

### 3.4 实体基类

```java
@MappedSuperclass
@EntityListeners(AuditingEntityListener.class)
public abstract class BaseEntity {
    @Id
    private String id;              // UUID

    @CreatedDate
    private LocalDateTime createdAt;

    @LastModifiedDate
    private LocalDateTime updatedAt;

    @CreatedBy
    private String createdBy;

    @LastModifiedBy
    private String updatedBy;

    @Column(name = "DEL_FLAG")
    private String delFlag = "0";   // 逻辑删除
}
```

### 3.5 认证流程（Gateway 统一鉴权）

```
登录请求 ──▶ Nginx ──▶ Gateway（白名单放行）──▶ litemes-base AuthController.login()
                │
                ▼
         SecurityService.authenticate()
                │
                ├── 验证用户名密码
                ├── 生成 JWT Token（有效期 24h，含 userId/role）
                └── 返回 Token + 用户信息

后续请求 ──▶ Nginx ──▶ Gateway AuthGlobalFilter（统一鉴权，第一道防线）
                │
                ├── 白名单路径直接放行（登录、Swagger 等，配置在 Nacos）
                ├── 从 Header 提取 Token，校验签名与有效期
                ├── 检查 Redis 黑名单（支持登出后强制下线）
                ├── 校验失败 → 直接返回 401，请求不会到达下游服务
                └── 校验通过 → 注入 X-User-Id / X-User-Role 头后按路由转发

下游服务 ──▶ JwtAuthFilter（common 模块提供，兜底防线）
                │
                ├── 优先读取网关注入的身份 Header（信任内部流量）
                ├── 同时支持 Feign 透传的 Token 校验（服务间调用不走网关）
                └── 设置 SecurityContext，放行到 Controller

Feign 内部调用 ──▶ FeignRequestInterceptor（common 模块）
                │
                └── 把当前请求的身份信息透传给被调服务，保证调用链上身份不丢失
```

> 设计要点（网关鉴权过滤的面试考点，都要能讲）：
> - 网关统一拦截未认证流量，下游服务只做轻量兜底，避免每个服务重复完整校验逻辑
> - Feign 内部调用不走网关，所以 common 模块保留 Token 透传拦截器 + 下游轻量校验，两条链路身份都不丢
> - GlobalFilter 的执行顺序（@Order）、白名单配置化（放 Nacos）、身份通过 Header 传递而非重新签发 Token，都是标准考点
> - WebSocket 握手不能自定义 Header，Token 放 query 参数过网关鉴权

### 3.6 WebSocket 设计

```
客户端                          服务端
  │                               │
  │──── WS 连接 /ws/production ──▶│
  │                               │
  │◀─── 连接成功确认 ─────────────│
  │                               │
  │     （工人报工触发事件）         │
  │                               │
  │◀─── 推送: TASK_COMPLETED ────│  { taskId, workOrderId, progress }
  │◀─── 推送: ORDER_PROGRESS ────│  { workOrderId, completedQty, percentage }
  │                               │
  │     （心跳保活，每 30s）        │
  │──── ping ────────────────────▶│
  │◀─── pong ─────────────────────│
  │                               │
  │     （断线重连，指数退避）       │
  │──── WS 重连 ─────────────────▶│
  │◀─── 全量数据推送 ─────────────│  补偿断线期间的变更
```

**事件类型**：

| 事件 | 触发时机 | 推送内容 |
|------|---------|---------|
| TASK_STARTED | 任务开始加工 | 任务 ID、工位信息 |
| TASK_COMPLETED | 任务自动完成（累计报工达到派工数量） | 任务 ID、完工数量、工单进度 |
| TASK_CLOSED | 工单关闭导致任务一并关闭 | 任务 ID、所属工单 ID |
| ORDER_STATUS_CHANGED | 工单状态变更 | 工单 ID、新状态 |
| WORKCENTER_STATUS_CHANGED | 工位状态变更 | 工位 ID、新状态 |

---

## 4. 前端架构详细设计

### 4.1 工程结构

```
lite-mes-ui/
├── package.json
├── vite.config.js
├── index.html
├── .env.development               # 开发环境变量
├── .env.production                # 生产环境变量
│
└── src/
    ├── main.js                    # 入口
    ├── App.vue                    # 根组件
    │
    ├── api/                       # 接口层
    │   ├── request.js             # Axios 封装（拦截器、Token、错误处理）
    │   ├── auth.js                # 认证接口
    │   ├── product.js             # 产品接口
    │   ├── process.js             # 工艺接口
    │   ├── workcenter.js          # 工位接口
    │   ├── workorder.js           # 工单接口
    │   ├── dispatch.js            # 派工接口
    │   └── dashboard.js           # 看板接口
    │
    ├── router/
    │   └── index.js               # 路由定义 + 导航守卫
    │
    ├── stores/                    # Pinia 状态管理
    │   ├── user.js                # 用户状态（Token、用户信息）
    │   └── app.js                 # 应用状态（侧边栏折叠等）
    │
    ├── views/                     # 页面
    │   ├── login/
    │   │   └── index.vue
    │   ├── layout/
    │   │   ├── index.vue          # 主布局（侧边栏 + 头部 + 内容区）
    │   │   ├── Sidebar.vue
    │   │   └── Navbar.vue
    │   ├── dashboard/
    │   │   └── index.vue          # 生产看板
    │   ├── product/
    │   │   ├── index.vue          # 产品列表
    │   │   └── components/
    │   │       └── ProductForm.vue
    │   ├── process/
    │   │   ├── index.vue          # 工艺路线编辑器（核心页面）
    │   │   └── components/
    │   │       ├── FlowCanvas.vue  # 画布组件（Vue Flow）
    │   │       ├── StepPanel.vue   # 工序列表面板
    │   │       ├── StepNode.vue    # 自定义工序节点
    │   │       └── PropertyPanel.vue # 属性面板
    │   ├── workcenter/
    │   │   ├── index.vue
    │   │   └── components/
    │   │       └── WorkCenterForm.vue
    │   ├── workorder/
    │   │   ├── index.vue          # 工单列表
    │   │   ├── detail.vue         # 工单详情
    │   │   └── components/
    │   │       └── WorkOrderForm.vue
    │   ├── dispatch/
    │   │   ├── index.vue          # 派工列表
    │   │   └── components/
    │   │       └── DispatchDialog.vue  # 派工弹窗
    │   ├── my-tasks/
    │   │   └── index.vue          # 操作员我的任务
    │   └── system/
    │       └── user.vue           # 用户管理（CRUD + 重置密码）
    │
    ├── components/                # 公共组件
    │   ├── Pagination.vue         # 分页组件
    │   ├── StatusTag.vue          # 状态标签（颜色映射）
    │   └── ConfirmButton.vue      # 确认按钮（二次确认）
    │
    ├── utils/
    │   ├── auth.js                # Token 存取
    │   ├── format.js              # 格式化工具
    │   └── websocket.js           # WebSocket 客户端封装
    │
    └── assets/
        └── styles/
            ├── variables.scss     # 主题变量
            └── global.scss        # 全局样式
```

### 4.2 路由设计

```javascript
const routes = [
  {
    path: '/login',
    component: () => import('@/views/login/index.vue'),
    meta: { public: true }          // 无需认证
  },
  {
    path: '/',
    component: Layout,              // 主布局
    redirect: '/dashboard',
    children: [
      { path: 'dashboard',    name: 'Dashboard',    component: () => import('@/views/dashboard/index.vue') },
      { path: 'product',      name: 'Product',      component: () => import('@/views/product/index.vue') },
      { path: 'process/:productId', name: 'Process', component: () => import('@/views/process/index.vue') },
      { path: 'workcenter',   name: 'WorkCenter',   component: () => import('@/views/workcenter/index.vue') },
      { path: 'workorder',    name: 'WorkOrder',    component: () => import('@/views/workorder/index.vue') },
      { path: 'workorder/:id', name: 'WorkOrderDetail', component: () => import('@/views/workorder/detail.vue') },
      { path: 'dispatch',     name: 'Dispatch',     component: () => import('@/views/dispatch/index.vue') },
      { path: 'my-tasks',     name: 'MyTasks',      component: () => import('@/views/my-tasks/index.vue') },
      { path: 'system/user',  name: 'SystemUser',   component: () => import('@/views/system/user.vue') },
    ]
  }
]
```

**导航守卫**：
- 未登录访问非 public 页面 → 跳转登录页
- 已登录访问登录页 → 跳转首页
- Token 过期 → 清除状态，跳转登录页

### 4.3 Axios 封装

```javascript
// request.js 核心逻辑
const service = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 15000
})

// 请求拦截：自动附加 Token
service.interceptors.request.use(config => {
  const token = getToken()
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

// 响应拦截：统一错误处理
service.interceptors.response.use(
  response => {
    const { code, message, data } = response.data
    if (code === 200) return data
    ElMessage.error(message || '请求失败')
    return Promise.reject(new Error(message))
  },
  error => {
    if (error.response?.status === 401) {
      // Token 过期，跳转登录
      store.dispatch('user/logout')
      router.push('/login')
    }
    ElMessage.error(error.message || '网络错误')
    return Promise.reject(error)
  }
)
```

### 4.4 工艺路线编辑器技术方案

这是前端最复杂的页面，基于 **Vue Flow** 实现：

```
┌─────────────────────────────────────────────────────┐
│                    编辑器页面                          │
│                                                     │
│  ┌──────────┐  ┌────────────────────┐  ┌─────────┐ │
│  │StepPanel │  │    FlowCanvas      │  │Property │ │
│  │          │  │                    │  │ Panel   │ │
│  │ 工序列表  │  │  Vue Flow 实例     │  │         │ │
│  │ 可拖拽   │──▶│  自定义节点        │──▶│ 属性编辑 │ │
│  │ 新增/删除 │  │  连线管理          │  │ 实时保存 │ │
│  │          │  │  拖拽定位          │  │         │ │
│  └──────────┘  └────────────────────┘  └─────────┘ │
│                                                     │
│  数据流：                                            │
│  1. 页面加载 → 调用 API 获取工艺路线 + 工序 + 连线     │
│  2. 转换为 Vue Flow 的 nodes + edges 格式渲染         │
│  3. 用户拖拽/编辑 → 更新本地 state                    │
│  4. 点击保存 → 批量调用 API 更新工序位置和连线          │
└─────────────────────────────────────────────────────┘
```

**自定义节点（StepNode）**：

```vue
<template>
  <div class="step-node" :class="{ selected: selected }">
    <div class="step-header">
      <span class="step-order">{{ data.stepOrder }}</span>
      <span class="step-name">{{ data.stepName }}</span>
    </div>
    <div class="step-meta">
      <span>{{ data.centerType || '未指定工位' }}</span>
      <span>{{ data.estimatedMinutes }}min</span>
    </div>
  </div>
</template>
```

---

## 5. 数据库设计

### 5.1 数据库选型与拆分策略

MySQL 8.0，理由：
- 最广泛使用，面试通用
- 免费开源
- 支持窗口函数、JSON 字段（后续扩展可能用到）

**库拆分（微服务核心约束：每个服务只访问自己的库）**：

| 库名 | 归属服务 | 包含表 |
|------|---------|--------|
| litemes_base | litemes-base | sys_user、product、workflow、workflow_step、workflow_step_link、work_center |
| litemes_production | litemes-production | work_order、dispatch_task |

> 学习项目用一个 MySQL 实例建两个库即可（省内存），但连接配置严格隔离；面试可讲"物理上已是分库，后续可直接拆到两个实例"。
> 跨库 JOIN 禁止：跨服务关联数据一律通过 OpenFeign 接口获取后在内存组装（如看板页聚合产品名、工位名）。

### 5.2 完整 DDL

#### 5.2.1 litemes_base 库（基础数据服务）

```sql
-- ========================================
-- 库 1：litemes_base（基础数据服务专属）
-- ========================================
CREATE DATABASE IF NOT EXISTS litemes_base DEFAULT CHARSET utf8mb4;
USE litemes_base;

-- 用户表
CREATE TABLE sys_user (
    id              VARCHAR(36)  NOT NULL PRIMARY KEY,
    username        VARCHAR(255) NOT NULL UNIQUE,
    password        VARCHAR(255) NOT NULL,
    real_name       VARCHAR(255) NOT NULL,
    role            VARCHAR(255) NOT NULL DEFAULT 'VIEWER',
    status          VARCHAR(255) NOT NULL DEFAULT 'ACTIVE',
    del_flag        CHAR(1)     NOT NULL DEFAULT '0',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by      VARCHAR(36),
    updated_by      VARCHAR(36),
    INDEX idx_role (role),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 产品表
CREATE TABLE product (
    id              VARCHAR(36)  NOT NULL PRIMARY KEY,
    product_code    VARCHAR(255) NOT NULL UNIQUE,
    product_name    VARCHAR(255) NOT NULL,
    description     TEXT,
    drawing_url     VARCHAR(2000),
    status          VARCHAR(255) NOT NULL DEFAULT 'ACTIVE',
    del_flag        CHAR(1)     NOT NULL DEFAULT '0',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by      VARCHAR(36),
    updated_by      VARCHAR(36),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 工艺路线表
CREATE TABLE workflow (
    id              VARCHAR(36)  NOT NULL PRIMARY KEY,
    product_id      VARCHAR(36)  NOT NULL,
    version         INT          NOT NULL DEFAULT 1,
    version_name    VARCHAR(255) NOT NULL,
    description     VARCHAR(2000),
    is_active       TINYINT(1)   NOT NULL DEFAULT 0,
    status          VARCHAR(255) NOT NULL DEFAULT 'DRAFT',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by      VARCHAR(36),
    updated_by      VARCHAR(36),
    CONSTRAINT fk_workflow_product FOREIGN KEY (product_id) REFERENCES product(id),
    INDEX idx_product (product_id),
    UNIQUE KEY uk_product_version (product_id, version)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 工序步骤表
CREATE TABLE workflow_step (
    id              VARCHAR(36)  NOT NULL PRIMARY KEY,
    workflow_id     VARCHAR(36)  NOT NULL,
    step_code       VARCHAR(255) NOT NULL,
    step_name       VARCHAR(255) NOT NULL,
    step_order      INT          NOT NULL,
    description     VARCHAR(2000),
    required_wc_type VARCHAR(255),
    estimated_minutes INT,
    pos_x           DOUBLE       NOT NULL DEFAULT 0,
    pos_y           DOUBLE       NOT NULL DEFAULT 0,
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    CONSTRAINT fk_step_workflow FOREIGN KEY (workflow_id) REFERENCES workflow(id),
    INDEX idx_workflow (workflow_id),
    UNIQUE KEY uk_workflow_step_code (workflow_id, step_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 工序连线表（前驱后继关系）
CREATE TABLE workflow_step_link (
    id              VARCHAR(36)  NOT NULL PRIMARY KEY,
    workflow_id     VARCHAR(36)  NOT NULL,
    source_step_id  VARCHAR(36)  NOT NULL,
    target_step_id  VARCHAR(36)  NOT NULL,
    CONSTRAINT fk_link_workflow FOREIGN KEY (workflow_id) REFERENCES workflow(id),
    CONSTRAINT fk_link_source FOREIGN KEY (source_step_id) REFERENCES workflow_step(id),
    CONSTRAINT fk_link_target FOREIGN KEY (target_step_id) REFERENCES workflow_step(id),
    UNIQUE KEY uk_link_pair (source_step_id, target_step_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 工位表
CREATE TABLE work_center (
    id              VARCHAR(36)  NOT NULL PRIMARY KEY,
    center_code     VARCHAR(255) NOT NULL UNIQUE,
    center_name     VARCHAR(255) NOT NULL,
    center_type     VARCHAR(255) NOT NULL,
    operator_id     VARCHAR(36),
    status          VARCHAR(255) NOT NULL DEFAULT 'IDLE',
    del_flag        CHAR(1)     NOT NULL DEFAULT '0',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    CONSTRAINT fk_wc_operator FOREIGN KEY (operator_id) REFERENCES sys_user(id),
    INDEX idx_status (status),
    INDEX idx_type (center_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 编号序列改为 Redis INCR 实现（key: seq:WORK_ORDER:{yyyyMMdd}），不再建表
```

#### 5.2.2 litemes_production 库（业务数据服务）

```sql
-- ========================================
-- 库 2：litemes_production（业务数据服务专属）
-- 注意：外键只保留同库内的（工单→任务），
-- 跨库引用（product_id、workflow_step_id 等）只存 ID，不做物理外键
-- ========================================
CREATE DATABASE IF NOT EXISTS litemes_production DEFAULT CHARSET utf8mb4;
USE litemes_production;

-- 生产工单表
CREATE TABLE work_order (
    id              VARCHAR(36)  NOT NULL PRIMARY KEY,
    order_no        VARCHAR(255) NOT NULL UNIQUE,
    product_id      VARCHAR(36)  NOT NULL,
    workflow_id     VARCHAR(36)  NOT NULL,
    quantity        INT          NOT NULL,
    completed_qty   INT          NOT NULL DEFAULT 0,
    priority        INT          NOT NULL DEFAULT 3,
    status          VARCHAR(255) NOT NULL DEFAULT 'PLANNED',
    plan_start_date DATE,
    plan_end_date   DATE,
    actual_start_date DATE,
    actual_end_date DATE,
    remark          VARCHAR(2000),
    del_flag        CHAR(1)     NOT NULL DEFAULT '0',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by      VARCHAR(36),
    updated_by      VARCHAR(36),
    -- product_id / workflow_id 跨库引用，不加物理外键，由 Feign 接口在创建时校验
    INDEX idx_status (status),
    INDEX idx_plan_end (plan_end_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- 派工任务表
CREATE TABLE dispatch_task (
    id              VARCHAR(36)  NOT NULL PRIMARY KEY,
    task_no         VARCHAR(255) NOT NULL UNIQUE,
    work_order_id   VARCHAR(36)  NOT NULL,
    workflow_step_id VARCHAR(36) NOT NULL,
    work_center_id  VARCHAR(36),
    operator_id     VARCHAR(36),
    quantity        INT          NOT NULL,
    completed_qty   INT          NOT NULL DEFAULT 0,
    status          VARCHAR(255) NOT NULL DEFAULT 'PENDING',
    dispatched_at   DATETIME,
    started_at      DATETIME,
    completed_at    DATETIME,
    remark          VARCHAR(2000),
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by      VARCHAR(36),
    updated_by      VARCHAR(36),
    CONSTRAINT fk_dt_workorder FOREIGN KEY (work_order_id) REFERENCES work_order(id),
    -- workflow_step_id / work_center_id / operator_id 均为跨库引用，不加物理外键
    INDEX idx_workorder (work_order_id),
    INDEX idx_operator (operator_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 5.3 初始化种子数据

```sql
-- litemes_base 库：默认管理员（密码: admin123，BCrypt 加密）
INSERT INTO sys_user (id, username, password, real_name, role, status)
VALUES ('01910000-0000-0000-0000-000000000001', 'admin',
        '$2a$10$...BCryptHash...', '系统管理员', 'ADMIN', 'ACTIVE');

-- litemes_base 库：演示数据——工位
INSERT INTO work_center (id, center_code, center_name, center_type, status) VALUES
('01910000-0000-0000-0000-000000000010', 'WC-LATHE-01', '车床-01', '车床', 'IDLE'),
('01910000-0000-0000-0000-000000000011', 'WC-LATHE-02', '车床-02', '车床', 'IDLE'),
('01910000-0000-0000-0000-000000000012', 'WC-MILL-01', '铣床-01', '铣床', 'IDLE'),
('01910000-0000-0000-0000-000000000013', 'WC-GRIND-01', '磨床-01', '磨床', 'IDLE'),
('01910000-0000-0000-0000-000000000014', 'WC-QC-01', '检验台-01', '检验台', 'IDLE');
```

```bash
# Redis 编号序列初始化（应用首次启动时由 CodeGenerator 自动初始化，无需手工执行）
SET seq:WORK_ORDER:20260826 0
SET seq:DISPATCH_TASK:20260826 0
# 生成规则：INCR 后取序号，拼接为 WO-20260826-001；key 过期时间设 2 天自动清理
```

### 5.4 Redis 使用规划（微服务下的 Redis 职责清单）

| Key 前缀 | 用途 | 所属服务 | TTL |
|---------|------|---------|-----|
| `jwt:blacklist:{token}` | 登出后的 Token 黑名单 | 网关鉴权过滤主读，下游服务兜底共读 | 剩余 Token 有效期 |
| `seq:WORK_ORDER:{date}` | 工单编号日序号（INCR） | production | 2 天 |
| `seq:DISPATCH_TASK:{date}` | 任务编号日序号（INCR） | production | 2 天 |
| `lock:report:{taskId}` | 报工并发锁（Redisson） | production | 自动释放 |
| `dashboard:summary` | 看板概览缓存 | production | 30s |
| `cache:product:{id}` | 产品详情缓存（Feign 调用前缓存） | production | 10 分钟 |
| `cache:workflow:{id}` | 工艺路线快照缓存（工单锁定版本查询） | production | 10 分钟 |

---

## 6. 部署方案

### 6.1 Docker Compose 编排

```yaml
# docker-compose.yml
version: '3.8'

services:
  nginx:
    image: nginx:1.25-alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./lite-mes-ui/dist:/usr/share/nginx/html:ro
    depends_on:
      - litemes-gateway
    restart: always

  litemes-gateway:
    build:
      context: .
      dockerfile: docker/Dockerfile.gateway
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - NACOS_ADDR=nacos:8848
    depends_on:
      nacos:
        condition: service_healthy
      litemes-base:
        condition: service_started
      litemes-production:
        condition: service_started
    restart: always

  litemes-base:
    build:
      context: .
      dockerfile: docker/Dockerfile.base
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - NACOS_ADDR=nacos:8848
    depends_on:
      nacos:
        condition: service_healthy
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always

  litemes-production:
    build:
      context: .
      dockerfile: docker/Dockerfile.production
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - NACOS_ADDR=nacos:8848
    depends_on:
      nacos:
        condition: service_healthy
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: always

  nacos:
    image: nacos/nacos-server:v2.3.2
    environment:
      - MODE=standalone
      - SPRING_DATASOURCE_PLATFORM=mysql
      - MYSQL_SERVICE_HOST=mysql
      - MYSQL_SERVICE_DB_NAME=nacos_config
      - MYSQL_SERVICE_USER=${DB_USERNAME:-root}
      - MYSQL_SERVICE_PASSWORD=${DB_PASSWORD}
      - JVM_XMS=256m
      - JVM_XMX=256m
    volumes:
      - nacos-data:/home/nacos/data
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8848/nacos/v1/console/health/readiness"]
      interval: 10s
      timeout: 5s
      retries: 12
    restart: always

  mysql:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}
      - MYSQL_DATABASE=nacos_config
      - MYSQL_CHARSET=utf8mb4
    volumes:
      - mysql-data:/var/lib/mysql
      - ./backups:/backups
      - ./sql/init-databases.sql:/docker-entrypoint-initdb.d/init.sql:ro   # 初始化：Nacos 表结构（nacos-mysql.sql）+ litemes_base / litemes_production 两库
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always

volumes:
  mysql-data:
  redis-data:
  nacos-data:
```

> Nacos 配置导入：首次启动后在 Nacos 控制台（默认 nacos/nacos）导入四个配置文件：`litemes-gateway.yml`、`litemes-base.yml`、`litemes-production.yml`、`application-common.yml`（数据库地址、Redis 密码、JWT 密钥等敏感信息全部收口到 Nacos，不再散落在容器环境变量）。
> 注意：Nacos 使用 MySQL 持久化时不会自动建表，`init-databases.sql` 必须包含官方 nacos-mysql.sql 的表结构，否则 nacos 容器首次启动会失败。

### 6.2 Nginx 配置（只对接网关）

```nginx
upstream gateway_service {
    server litemes-gateway:8080;
}

server {
    listen 80;
    server_name _;

    # 前端静态资源
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }

    # 所有 API 统一交给网关，由网关完成路由与鉴权
    location /api/ {
        proxy_pass http://gateway_service;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # WebSocket 代理（同样经网关路由到 production 服务）
    location /ws/ {
        proxy_pass http://gateway_service;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
    }
}
```

> API 路径约定不变：base 服务 `/api/v1/base/**`，production 服务 `/api/v1/prod/**`；路由与鉴权都由网关完成，前端和 Nginx 均无需感知后端服务拓扑。

### 6.3 后端 Dockerfile（网关/base/production 各一个，结构相同）

```dockerfile
# docker/Dockerfile.base（gateway/production 同理，拷贝对应模块的 jar）
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY litemes-base/target/litemes-base-*.jar app.jar
EXPOSE 8081
# 2C 云服务器上限制 JVM 内存，防止与 Nacos/MySQL 抢内存
ENTRYPOINT ["java", "-Xms256m", "-Xmx512m", "-jar", "app.jar"]
```

### 6.4 云服务器部署步骤

```bash
# 1. 安装 Docker + Docker Compose
# 2. 上传项目文件
scp -r lite-mes/ root@your-server:/opt/lite-mes/

# 3. 配置环境变量
cd /opt/lite-mes
cp .env.example .env
vim .env   # 填写密码和密钥

# 4. 构建后端（Maven 多模块，根目录一条命令）
mvn clean package -DskipTests

# 5. 构建前端
cd lite-mes-ui
npm install && npm run build
cd ..

# 6. 启动全部服务
docker compose up -d

# 7. 初始化 Nacos 配置（首次）
#    打开 http://your-server:8848/nacos，导入四个配置文件

# 8. 验证
curl http://your-server/api/v1/base/auth/me
```

---

## 7. 开发计划

### 7.1 里程碑

| 阶段 | 内容 | 预估时间 |
|------|------|---------|
| M1 - 骨架搭建 | Maven 多模块初始化、Nacos 环境、Gateway 路由与鉴权、双库建表、认证模块、主布局 | 4 天 |
| M2 - 基础数据 | 产品管理、工位管理、工艺建模（含流程图编辑器） | 5 天 |
| M3 - 生产核心 | 工单管理、派工管理、报工（含 Feign 跨服务调用与删除保护） | 5 天 |
| M4 - 看板与实时 | 生产看板（Feign 聚合 + Redis 缓存）、WebSocket 实时推送 | 3 天 |
| M5 - 打磨部署 | 数据校验完善、UI 美化、Docker Compose 部署、文档 | 3 天 |
| **合计** | | **约 20 天** |

### 7.2 优先级排序

```
必须做（MVP）:
  Nacos 环境 → Gateway → 认证 → 产品 → 工艺编辑器 → 工位 → 工单 → 派工 → 看板
  （Gateway 鉴权过滤与 Feign 跨服务调用贯穿 M1–M4，是微服务改造的核心练习点）

可以后做:
  WebSocket 实时 → 报工详情 → 操作员专属页面 → 数据导出 → Sentinel 限流降级

不需要做:
  消息队列 → 搜索引擎 → 文件存储 → 链路追踪（SkyWalking 留作二期演进点）
```

---

## 8. 简历描述建议

```
LiteMES 轻量级制造执行系统（微服务版）
━━━━━━━━━━━━━━━━━━━━━━━
技术栈：Spring Cloud + Spring Cloud Gateway + Nacos + OpenFeign + Spring Boot 3.2 + Vue 3 + MySQL + Redis + WebSocket + Docker
项目描述：面向小型加工厂的轻量 MES 系统，采用微服务架构按"基础数据 / 业务数据"
         拆分为两个独立服务，Gateway 统一入口鉴权，支持工艺路线可视化设计、
         生产工单全生命周期管理、工位派工与实时进度看板。
核心亮点：
  • 微服务架构：Nacos 注册发现 + 配置中心，OpenFeign + LoadBalancer 服务间调用，
    数据库按服务分库，跨服务关联通过契约接口内存组装，禁止跨库 JOIN
  • Gateway 统一入口：基于 GlobalFilter 实现 JWT 全局鉴权过滤 + Redis 黑名单强制下线，
    lb:// 动态路由对接服务发现，跨域与白名单统一收口
  • 基于 Vue Flow 实现工艺路线拖拽式可视化编辑器
  • Redis 多场景应用：JWT 黑名单、Redis INCR 分布式编号生成、Redisson 报工并发锁、看板缓存
  • WebSocket 实时推送生产事件，看板秒级刷新
  • JWT 认证 + RBAC 权限模型，网关统一鉴权 + 下游服务兜底校验，Feign 拦截器透传身份
  • Docker Compose 一键部署：Nginx + Gateway + 双服务 + Nacos + MySQL 双库 + Redis
  • 基于真实制造业业务经验设计，非 CRUD 玩具项目
在线地址：https://your-domain.com
```

**面试追问预案（简历上每一条都要能展开讲）**：

| 可能的追问 | 答题要点 |
|-----------|---------|
| 为什么拆两个服务而不是更多？ | 按主数据 vs 交易数据的读写特征拆；过度拆分增加运维成本，两个服务足以展示微服务核心能力 |
| 网关具体做了哪些事？ | 三件事：① 路由转发（Path 谓词 + lb:// 动态服务发现，服务扩容无需改配置）；② JWT 全局鉴权过滤（GlobalFilter 校验签名/过期/Redis 黑名单，未认证请求到不了下游）；③ 跨域统一处理，下游服务不再各自配 CORS |
| 网关鉴权和下游校验是什么关系？ | 网关是第一道防线拦截外部流量；下游保留轻量校验作为兜底，主要处理 Feign 内部调用（不走网关）和服务被直接访问的场景 |
| 服务间怎么通信？失败了怎么办？ | OpenFeign 声明式调用 + LoadBalancer；fallback 兜底返回降级结果，主数据查询配合 Redis 缓存兜底 |
| 分布式事务怎么处理？ | 派工→更新工位状态跨两个服务：采用"本地事务 + 接口幂等 + 失败重试"的最终一致方案，避免引入 Seata 的复杂度 |
| 编号怎么保证不重复？ | Redis INCR 原子自增 + 日期 key，比数据库序列表性能高且天然支持多实例 |
| WebSocket 怎么过网关鉴权？ | WS 握手不能自定义 Header，Token 放 query 参数，网关鉴权通过后转发到 production 服务 |
