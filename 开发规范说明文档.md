# LiteMES 开发规范说明文档

> 版本：v1.0 | 日期：2026-08-26 | 配套文档：《架构设计说明书.md》《设计规格说明书.md》

适用范围：`litemes-common` / `litemes-api` / `litemes-gateway` / `litemes-base` / `litemes-production` 后端模块与 `lite-mes-ui` 前端工程。

---

## 1. 总则

本规范只保证两条一致性：

1. **前端页面风格一致** —— 同一类页面（列表、表单、详情、看板）在任何模块长得一样、交互一样。
2. **后端接口逻辑一致** —— 同一类操作（查询、创建、更新、删除、状态流转）在任何服务里流程一样、响应一样。

规则优先级：**设计文档 > 本规范 > 个人习惯**。与设计文档冲突时以《设计规格说明书》《架构设计说明书》为准。

---

## 2. 通用约定

| 项 | 约定 |
|----|------|
| 语言 | 代码标识符用英文；注释、提交信息、文档用中文 |
| 主键 | `String` 类型 UUID（36 位），代码生成，禁止数据库自增 |
| 日期时间 | 接口传输格式 `yyyy-MM-dd HH:mm:ss`，纯日期 `yyyy-MM-dd` |
| 逻辑删除 | `delFlag = "0"/"1"`，所有查询默认过滤 `delFlag = "0"`，删除一律逻辑删除，禁止物理删除 |
| 状态值 | 所有状态字段用枚举类或常量集中定义，**禁止魔法字符串散落在业务代码里** |
| 审计字段 | `createdAt / updatedAt / createdBy / updatedBy` 由 JPA Auditing 自动填充，禁止手写 |

---

## 3. 后端开发规范

### 3.1 分层职责

| 层 | 职责 | 禁止 |
|----|------|------|
| Controller | 参数校验、调用 Service、返回 AjaxResult | 写业务逻辑、直接调 Repository |
| Service | 业务逻辑、事务、状态机校验 | 接触 HttpServletRequest 等 Web 对象 |
| Repository | 数据访问、JPQL 查询 | 包含业务逻辑 |
| Entity | 数据库表映射，继承 BaseEntity | 出现在 Controller 参数/返回值 |
| DTO / VO | 接口入参 / 只读展示对象 | 暴露 Entity 内部结构 |

### 3.2 接口路径与响应

**路径规则**：

- 对外 API：`/api/v1/base/**`（基础数据服务）、`/api/v1/prod/**`（业务数据服务）
- 内部契约：`/inner/**`（仅供 Feign 调用，不对前端暴露，不走网关路由）
- WebSocket：`/ws/production`

**统一响应体 AjaxResult**：

```json
{ "code": 200, "message": "success", "data": { } }
```

| code | 含义 | 使用场景 |
|------|------|---------|
| 200 | 成功 | 一切正常 |
| 400 | 参数错误 | DTO 校验失败、业务规则不满足 |
| 401 | 未认证 | Token 缺失/过期（网关已拦，下游兜底） |
| 403 | 无权限 | 角色不匹配 |
| 404 | 资源不存在 | 详情/更新/删除目标不存在 |
| 500 | 系统错误 | 未捕获异常 |

**分页响应**（放在 data 内）：

```json
{ "rows": [ ], "total": 0, "pageNum": 1, "pageSize": 20 }
```

- `pageNum` 从 1 开始；`pageSize` 默认 20、上限 100
- 列表默认按 `createdAt DESC` 排序
- 空列表时 `rows` 必须返回 `[]`，禁止返回 null

### 3.3 标准接口流程（逻辑一致的核心）

每个接口必须按下面的模板实现，跨服务完全统一：

| 操作 | 标准流程 |
|------|---------|
| 创建 | DTO 校验 → 唯一性校验（编码类字段查重）→ 编号生成（工单/任务走 CodeGenerator）→ 跨服务校验（Feign）→ 保存 → 返回完整详情 |
| 更新 | 按 ID 查实体（不存在返回 404）→ 状态机校验 → 更新允许修改的字段 → 返回详情。主键、编号、审计字段、创建人禁止更新 |
| 删除 | 查存在 → 引用检查（按《设计规格说明书》8.4 删除保护表）→ 逻辑删除 → 返回 success |
| 状态流转 | 校验"当前状态 → 目标状态"符合状态机，非法流转返回 400 并说明原因；流转动作与业务副作用放同一事务（如关闭工单 → 任务一并置 CLOSED → 释放工位占用） |
| 列表查询 | 分页参数 → 筛选条件 → `delFlag = "0"` 过滤 → 跨服务字段经 Feign 补全名称 → 返回分页 |

**通用规则**：

- 写操作必须加 `@Transactional`
- 报工接口必须加 Redisson 锁（key：`lock:report:{taskId}`）
- 报工为累计式：`completedQty += 本次数量`，累计达到派工数量自动置 COMPLETED
- 接口须可安全重复调用（幂等）：报工重复提交靠锁 + 数量校验拦截；删除重复调用返回成功

### 3.4 状态机（只允许图中的流转）

- 工单：`PLANNED → RELEASED → IN_PROGRESS → COMPLETED`；`RELEASED / IN_PROGRESS → CLOSED`；仅 PLANNED 可删除
- 任务：`PENDING → PROCESSING → COMPLETED`；工单关闭时 `PENDING / PROCESSING → CLOSED`
- 禁止任何未画在《设计规格说明书》4.2 / 4.3 状态图中的流转

### 3.5 命名规范

| 对象 | 规则 | 示例 |
|------|------|------|
| 包 | `com.litemes.{服务}.module.{模块}` | `com.litemes.production.module.workorder` |
| Controller | XxxController | `WorkOrderController` |
| Service | XxxService | `WorkOrderService` |
| Repository | XxxRepository | `WorkOrderRepository` |
| Entity | 表名去下划线驼峰 | `WorkOrder`、`DispatchTask` |
| DTO / VO | XxxDTO（入参）/ XxxVO（出参） | `WorkOrderCreateDTO`、`WorkOrderVO` |
| 枚举 | 状态类集中放 module 下 `enums` 包 | `WorkOrderStatus` |
| 常量 | UPPER_SNAKE_CASE | `MAX_PAGE_SIZE` |

- 实体属性 camelCase，数据库列 snake_case（Hibernate 默认命名策略，不要额外配置）
- 方法命名：查询 `get / find / list / page`，创建 `create`，更新 `update`，删除 `delete`，流转 `release / close / start / report`

### 3.6 校验与异常

- 入参校验用 JSR-303 注解标在 DTO 上（`@NotBlank / @NotNull / @Min`），Controller 用 `@Valid` 触发
- 业务失败抛 `BusinessException(message)`，禁止抛裸 RuntimeException
- 异常统一由 GlobalExceptionHandler 转 AjaxResult，Controller 不写 try-catch
- 日志：接口入口记 INFO（操作人 + 关键参数），异常记 ERROR（含堆栈），禁止 System.out
- 日志中禁止打印密码、Token 等敏感信息

### 3.7 服务间调用

- Feign 接口契约统一定义在 `litemes-api` 模块（接口 + DTO），禁止两个服务各写一份
- **禁止跨库 JOIN**：跨服务数据经 Feign 获取后在内存组装
- 主数据查询优先走 Redis 缓存（`cache:product:{id}` 等）
- fallback 规则：删除保护类调用失败 → 直接拒绝（安全优先）；展示类调用失败 → 降级返回空，不阻塞主流程

### 3.8 Redis 使用规范

| Key 前缀 | 用途 | TTL |
|---------|------|-----|
| `jwt:blacklist:{token}` | 登出 Token 黑名单 | 剩余有效期 |
| `seq:WORK_ORDER:{yyyyMMdd}` | 工单编号日序号 | 2 天 |
| `seq:DISPATCH_TASK:{yyyyMMdd}` | 任务编号日序号 | 2 天 |
| `lock:report:{taskId}` | 报工并发锁 | 自动释放 |
| `dashboard:summary` | 看板概览缓存 | 30s |
| `cache:product:{id}` | 产品详情缓存 | 10 分钟 |
| `cache:workflow:{id}` | 工艺路线快照缓存 | 10 分钟 |

- 编号只能通过 common 的 CodeGenerator 生成，禁止业务代码直接 INCR
- 锁必须 try-finally 释放，禁止漏释放

---

## 4. 前端开发规范

### 4.1 工程结构（同《架构设计说明书》4.1）

- 接口层：每个模块一个 api 文件（`api/product.js` 等），全部走 `request.js` 封装，**禁止页面内直接 import axios**
- 状态管理：Pinia store，命名 `useXxxStore`
- 路由：统一定义在 `router/index.js`，路由 name 用 PascalCase
- 公共组件：`Pagination` / `StatusTag` / `ConfirmButton` 三件套必须使用，禁止各页面自造

### 4.2 页面风格统一规范（重点）

#### 4.2.1 列表页模板（所有列表页一致）

```
搜索栏（el-form inline：筛选条件 + [查询] [重置] 按钮）
表格（el-table：v-loading + 空状态"暂无数据"）
分页（Pagination 组件，固定表格底部右侧）
```

- 状态列一律用 StatusTag 组件，禁止手写 el-tag + 颜色映射
- 操作列固定右侧，按钮用 link 类型；超过 3 个操作时收进"更多"下拉
- 危险操作（删除、关闭）一律 ConfirmButton 二次确认

#### 4.2.2 状态标签颜色映射（全站唯一，维护在 StatusTag 组件内）

| 状态 | 颜色 | 状态 | 颜色 |
|------|------|------|------|
| PLANNED / DRAFT / PENDING / INACTIVE / CLOSED / OFFLINE | info（灰） | RELEASED | primary（蓝） |
| IN_PROGRESS / PROCESSING / BUSY / ARCHIVED | warning（橙） | COMPLETED / ACTIVE / IDLE | success（绿） |
| DISABLED | danger（红） | 优先级 1 / 2 / 3 / 4 | danger / warning / primary / info |

任何页面新增状态展示，先改这张表和 StatusTag，再引用。

#### 4.2.3 表单弹窗模板

- Dialog 宽度统一 560px（内容复杂的可放宽，全站一致原则下同模块统一）
- 表单 label-width 统一 100px
- 必填项用 rules 校验，必填星号自动显示
- 提交按钮加 loading，防重复提交
- 编辑回显、关闭时重置（destroy-on-close 或 resetFields）

#### 4.2.4 显示格式统一

- 日期时间 `YYYY-MM-DD HH:mm:ss`，纯日期 `YYYY-MM-DD`，空值显示 "—"
- 数量整数直接显示；进度百分比保留 1 位小数
- 数值类列（数量、百分比）右对齐

#### 4.2.5 消息提示统一

- 操作成功：`ElMessage.success("保存成功")`，句式"动词 + 成功"
- 操作失败：request.js 响应拦截器已统一弹错，页面**不再重复弹**
- 删除确认文案统一："确定删除该 XXX 吗？删除后不可恢复。"

#### 4.2.6 加载与空状态

- 请求期间 el-table 加 v-loading
- 空表格统一 `empty-text="暂无数据"`
- 看板等无数据页面用 el-empty 组件

### 4.3 命名与代码规范

- 组件文件 PascalCase（页面目录下 index.vue 除外），目录小写
- 方法/变量 camelCase，常量 UPPER_SNAKE_CASE（集中放 `utils/constants.js`）
- 一律使用 Vue 3 组合式 API（`<script setup>`），禁止 Options API 混写
- 状态枚举与颜色映射集中维护在 `utils/constants.js`，禁止页面里散写

### 4.4 样式规范

- 颜色、间距、圆角等走 `assets/styles/variables.scss` 变量，禁止硬编码
- 禁止内联 style（确需动态值的除外）
- Element Plus 主题变量覆盖统一在 variables.scss，禁止页面内用 :deep 改全局组件样式
- 每个页面根元素加 `page-xxx` 命名空间类，避免样式相互污染

### 4.5 WebSocket 规范

- 连接统一走 `utils/websocket.js` 封装，页面禁止裸 new WebSocket
- Token 放 query 参数（WS 握手不能自定义 Header）
- 事件类型见《架构设计说明书》3.6：TASK_STARTED / TASK_COMPLETED / TASK_CLOSED / ORDER_STATUS_CHANGED / WORKCENTER_STATUS_CHANGED
- 断线自动重连（指数退避），重连成功后全量拉取一次数据

---

## 5. 自查清单

提交代码前逐项核对：

**后端**：

- [ ] 分层正确，Controller 无业务逻辑
- [ ] 返回 AjaxResult，错误码正确（资源不存在用 404）
- [ ] DTO 有校验注解，Controller 有 @Valid
- [ ] 写操作有 @Transactional
- [ ] 状态流转符合 3.4 状态机
- [ ] 删除走逻辑删除 + 引用检查
- [ ] 编号走 CodeGenerator，锁 try-finally 释放
- [ ] Feign 契约定义在 litemes-api，无跨库 JOIN
- [ ] 无魔法字符串状态值，无敏感信息日志

**前端**：

- [ ] 请求走 request.js + api 模块文件
- [ ] 状态列用 StatusTag，颜色符合 4.2.2 映射表
- [ ] 分页用 Pagination 组件，空状态"暂无数据"
- [ ] 危险操作有 ConfirmButton 二次确认
- [ ] 日期/数量格式符合 4.2.4
- [ ] 失败提示不重复弹，成功提示句式统一
- [ ] 表单弹窗有 loading + 关闭重置
- [ ] 无内联样式，颜色走 variables.scss 变量
